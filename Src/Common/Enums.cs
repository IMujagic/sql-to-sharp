namespace SqlToSharp.Common
{
    public enum Dbms
    {
        SqlServer,
        Postgres
    }

    public enum LogMessageType
    {
        Info,
        Warning,
        Error,
        Exception
    }
}